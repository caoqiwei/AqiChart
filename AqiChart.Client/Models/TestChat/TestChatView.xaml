<UserControl x:Class="AqiChart.Client.Models.TestChat.TestChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
             xmlns:local="clr-namespace:AqiChart.Client.Models.TestChat"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Border Grid.Row="1" Background="#F3F3F3" BorderBrush="#DADADA" BorderThickness="0 0 1 0">

                <ListBox BorderThickness="0" ItemsSource="{Binding FriendList}" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="10 5">
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="55" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="40" />
                                </Grid.ColumnDefinitions>
                                <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Source="{Binding AvatarUrl}" Margin="5"></Image>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding NickName}" Foreground="#10141A" VerticalAlignment="Center" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"></TextBlock>
                                <TextBlock Grid.Row="0" Grid.Column="3" Text="09:36" Foreground="#9093A3" VerticalAlignment="Center" TextAlignment="Center"></TextBlock>
                                <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="When Recipe Save As, the original recipe name is displayed in the input box." 
                                TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Foreground="#9093A3" VerticalAlignment="Center"></TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Width="215" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
                <!--<ListView ItemsSource="{Binding FriendList, Mode=TwoWay}" Width="209" BorderThickness="0" >
                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </Style>
                </ListView.Resources>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        -->
                <!--
                        <Grid Margin="10 5">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="55" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="40" />
                            </Grid.ColumnDefinitions>
                            <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Source="{Binding AvatarUrl}" Margin="5"></Image>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding NickName}" Foreground="#10141A" VerticalAlignment="Center" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"></TextBlock>
                            <TextBlock Grid.Row="0" Grid.Column="3" Text="09:36" Foreground="#9093A3" VerticalAlignment="Center" TextAlignment="Center"></TextBlock>
                            <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="When Recipe Save As, the original recipe name is displayed in the input box." 
                                TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Foreground="#9093A3" VerticalAlignment="Center"></TextBlock>
                        </Grid>
                        -->
                <!--
                        <StackPanel Orientation="Horizontal" Height="50">
                            <Image Source="{Binding AvatarUrl}" Width="40" Height="40"/>
                            <StackPanel Margin="10,0">
                                <TextBlock Text="{Binding NickName}" FontWeight="Bold"/>
                                <TextBlock Text="When Recipe Save As, the original recipe name is displayed in the input box." Foreground="Gray"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Width="235" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>-->

            </Border>

        </Grid>


        <Grid Grid.Column="1" Background="White">

        </Grid>


    </Grid>
</UserControl>
