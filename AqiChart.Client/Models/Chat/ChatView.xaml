<UserControl x:Class="AqiChart.Client.Models.Chat.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
             xmlns:local="clr-namespace:AqiChart.Client.Models.Chat"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

        <local:ChatTemplateSelector x:Key="ChatTemplateSelector"></local:ChatTemplateSelector>
        


        <!-- 头像样式 -->
        <Style x:Key="AvatarStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>

        <!-- 消息框样式 -->
        <Style x:Key="MessageBubbleStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="MaxWidth" Value="280"/>
        </Style>

        <!-- 时间显示样式 -->
        <Style x:Key="TimeTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="Margin" Value="8,2"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="#F7F7F7">
        <Grid.RowDefinitions>
            <RowDefinition Height="68" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Border BorderBrush="#DADADA" BorderThickness="0 0 0 1">
            <Grid Margin="0 0 8 10" VerticalAlignment="Bottom" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="30" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding Friend.NickName}" FontSize="18"  Margin="10 0 10 0" />

                <!--<Button Grid.Column="1" Content="&#xe489;" Style="{StaticResource IconButton}" Width="26" Height="26"></Button>-->
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="250" />
            </Grid.RowDefinitions>

            <Border BorderBrush="#DADADA" BorderThickness="0 0 0 1" >
                <ScrollViewer VerticalScrollBarVisibility="Auto" BorderThickness="0">
                    <ItemsControl HorizontalContentAlignment="Stretch" ItemsSource="{Binding Chats}"
                        ItemTemplateSelector="{StaticResource ChatTemplateSelector}" 
                        x:Name="chats" Background="White" Margin="10">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel></StackPanel>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.Template>
                            <ControlTemplate TargetType="{x:Type ItemsControl}">
                                <Grid>
                                    <ItemsPresenter></ItemsPresenter>
                                </Grid>
                            </ControlTemplate>
                        </ItemsControl.Template>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="35" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBox Grid.Row="1" Text="{Binding Message}" Height="250" BorderThickness="0" BorderBrush="Transparent" Padding="5"
                     cal:Message.Attach="[Event KeyDown] = [Action MessageEnter($eventargs)]">

                </TextBox>

                <Button Grid.Row="1" Content="发送" cal:Message.Attach="SendMessage()" Width="96" Height="32" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 10 10"></Button>
            </Grid>

        </Grid>

    </Grid>
</UserControl>
