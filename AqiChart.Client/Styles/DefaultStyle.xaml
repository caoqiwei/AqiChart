<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:chatMessage="clr-namespace:AqiChart.Client.Models.Chat">

    <Style x:Key="WindowStyle" TargetType="{x:Type Window}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="AllowsTransparency" Value="True" />
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <!--GlassFrameThickness="0"-->
                <WindowChrome UseAeroCaptionButtons="False" CornerRadius="0" GlassFrameThickness="0" CaptionHeight="30"  />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}"  >
                        <Border.Effect>
                            <DropShadowEffect
                                Direction="0"
                                ShadowDepth="0" />
                        </Border.Effect>
                        <AdornerDecorator >
                            <ContentPresenter x:Name="win_content" />
                        </AdornerDecorator>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter Property="Margin" TargetName="win_content" Value="8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <DataTemplate x:Key="chatother" DataType="{x:Type chatMessage:ChatContent}">
        <StackPanel Orientation="Horizontal">
            <Ellipse Width="34" Height="34" ClipToBounds="True" SnapsToDevicePixels="True" VerticalAlignment="Top">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding AvatarUrl}"/>
                </Ellipse.Fill>
            </Ellipse>
            <Path Fill="#9EEA6A" Stretch="Uniform" Height="10" SnapsToDevicePixels="True" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10,16,-1,0">
                <Path.Data>
                    M316.906804 600.553647l369.209487 292.918955a166.898002 166.898002 0 0 1 65.243759 130.527398V0a167.017642 167.017642 0 0 1-65.243759 130.527398L316.906804 423.446353a110.707014 110.707014 0 0 0-22.093547 154.973867 110.667134 110.667134 0 0 0 22.093547 22.133427z
                </Path.Data>
            </Path>
            <Border Padding="10,10,10,10" Margin="0,2" VerticalAlignment="Center" 
                HorizontalAlignment="Left" CornerRadius="2" ClipToBounds="True" 
                SnapsToDevicePixels="True" Background="#9EEA6A">
                <StackPanel>
                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" MaxWidth="100" FontSize="14" Foreground="Black"></TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </DataTemplate>

    <!--send style-->
    <DataTemplate x:Key="chatowner" DataType="{x:Type chatMessage:ChatContent}">
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <Border Padding="10,10,10,10" Margin="0,2" VerticalAlignment="Top" 
                HorizontalAlignment="Right" CornerRadius="2" ClipToBounds="True" 
                SnapsToDevicePixels="True" Background="#9EEA6A">
                <StackPanel>
                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" MaxWidth="100" FontSize="14" Foreground="Black"></TextBlock>
                </StackPanel>
            </Border>
            <Path Fill="#9EEA6A" Stretch="Uniform" Height="10" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-1,16,10,0">
                <Path.Data>
                    M377.4780684453485 789.1111583709719l279.6549224853515-279.65492248535156-278.8075160980225-278.80751609802246z
                </Path.Data>
            </Path>
            <Ellipse Width="34" Height="34" VerticalAlignment="Top" ClipToBounds="True" SnapsToDevicePixels="True">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding AvatarUrl}"/>
                </Ellipse.Fill>
            </Ellipse>
        </StackPanel>
    </DataTemplate>


    <!-- 垂直滚动条样式 -->
    <Style x:Key="WeChatVerticalScrollBar" TargetType="{x:Type ScrollBar}">
        <Setter Property="Width" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid>
                        <Track x:Name="PART_Track" IsDirectionReversed="true">
                            <Track.Thumb>
                                <Thumb x:Name="Thumb">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="{x:Type Thumb}">
                                            <Border x:Name="ThumbBorder" CornerRadius="4" Background="Transparent">
                                                <Rectangle x:Name="ThumbRectangle" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" 
                                                    RadiusX="2" RadiusY="2" Fill="#B2B2B2" Opacity="0.6">
                                                    <Rectangle.Triggers>
                                                        <EventTrigger RoutedEvent="MouseEnter">
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Fill.(SolidColorBrush.Opacity)"
                                                                        To="0.8" Duration="0:0:0.2"/>
                                                                    <ColorAnimation Storyboard.TargetProperty="Fill.(SolidColorBrush.Color)"
                                                                        To="#808080" Duration="0:0:0.2"/>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </EventTrigger>
                                                        <EventTrigger RoutedEvent="MouseLeave">
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Fill.(SolidColorBrush.Opacity)"
                                                                        To="0.6" Duration="0:0:0.3"/>
                                                                    <ColorAnimation Storyboard.TargetProperty="Fill.(SolidColorBrush.Color)"
                                                                        To="#B2B2B2" Duration="0:0:0.3"/>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </EventTrigger>
                                                    </Rectangle.Triggers>
                                                </Rectangle>
                                            </Border>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 水平滚动条样式 -->
    <Style x:Key="WeChatHorizontalScrollBar" TargetType="{x:Type ScrollBar}">
        <Setter Property="Height" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid>
                        <Track x:Name="PART_Track">
                            <Track.Thumb>
                                <Thumb x:Name="Thumb">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="{x:Type Thumb}">
                                            <Border x:Name="ThumbBorder" 
                                                        CornerRadius="4" 
                                                        Background="Transparent">
                                                <Rectangle x:Name="ThumbRectangle"
                                                               Height="4"
                                                               HorizontalAlignment="Stretch"
                                                               VerticalAlignment="Center"
                                                               RadiusX="2"
                                                               RadiusY="2"
                                                               Fill="#B2B2B2"
                                                               Opacity="0.6">
                                                    <Rectangle.Triggers>
                                                        <EventTrigger RoutedEvent="MouseEnter">
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Fill.(SolidColorBrush.Opacity)"
                                                                                         To="0.8"
                                                                                         Duration="0:0:0.2"/>
                                                                    <ColorAnimation Storyboard.TargetProperty="Fill.(SolidColorBrush.Color)"
                                                                                        To="#808080"
                                                                                        Duration="0:0:0.2"/>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </EventTrigger>
                                                        <EventTrigger RoutedEvent="MouseLeave">
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Fill.(SolidColorBrush.Opacity)"
                                                                                         To="0.6"
                                                                                         Duration="0:0:0.3"/>
                                                                    <ColorAnimation Storyboard.TargetProperty="Fill.(SolidColorBrush.Color)"
                                                                                        To="#B2B2B2"
                                                                                        Duration="0:0:0.3"/>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </EventTrigger>
                                                    </Rectangle.Triggers>
                                                </Rectangle>
                                            </Border>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    
    <Style TargetType="{x:Type ScrollViewer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                            Grid.Column="0"
                            Grid.Row="0"
                            Margin="{TemplateBinding Padding}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"/>

                        <!-- 垂直滚动条 -->
                        <ScrollBar x:Name="PART_VerticalScrollBar"
                            Grid.Column="1"
                            Grid.Row="0"
                            Style="{StaticResource WeChatVerticalScrollBar}"
                            Value="{TemplateBinding VerticalOffset}"
                            Maximum="{TemplateBinding ScrollableHeight}"
                            ViewportSize="{TemplateBinding ViewportHeight}"
                            Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                        <!-- 水平滚动条 -->
                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                            Grid.Column="0"
                            Grid.Row="1"
                            Orientation="Horizontal"
                            Style="{StaticResource WeChatHorizontalScrollBar}"
                            Value="{TemplateBinding HorizontalOffset}"
                            Maximum="{TemplateBinding ScrollableWidth}"
                            ViewportSize="{TemplateBinding ViewportWidth}"
                            Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="CustomArrowExpander" TargetType="Expander">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 标题区域 -->
                            <ToggleButton x:Name="HeaderSite"
                                    Grid.Row="0"
                                    Content="{TemplateBinding Header}"
                                    ContentTemplate="{TemplateBinding HeaderTemplate}"
                                    IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                    Background="Transparent"
                                    BorderBrush="Transparent">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Padding="10,5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- 自定义箭头 -->
                                                <Path x:Name="Arrow"
                                                  Grid.Column="0"
                                                  Width="10"
                                                  Height="10"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Data="M 0 0 L 5 5 L 10 0"
                                                  Stroke="Black"
                                                  StrokeThickness="2">
                                                    <Path.RenderTransform>
                                                        <RotateTransform Angle="0" CenterX="5" CenterY="5"/>
                                                    </Path.RenderTransform>
                                                </Path>

                                                <ContentPresenter Grid.Column="1"
                                                            Margin="10,0,0,0"
                                                            Content="{TemplateBinding Content}"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Arrow" Property="RenderTransform">
                                                    <Setter.Value>
                                                        <RotateTransform Angle="180" CenterX="5" CenterY="5"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- 内容区域 -->
                            <ContentPresenter x:Name="ExpandSite"
                                        Grid.Row="1"
                                        Visibility="Collapsed"
                                        Margin="0"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 动画Expander样式 -->
    <Style x:Key="AnimatedExpander" TargetType="Expander">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- 标题按钮 -->
                            <ToggleButton x:Name="ExpanderButton"
                                        Grid.Row="0"
                                        IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        HorizontalContentAlignment="Stretch"
                                        Background="Transparent"
                                        BorderBrush="Transparent">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- 箭头图标 -->
                                    <Path x:Name="Arrow"
                                          Grid.Column="0"
                                          Width="10"
                                          Height="6"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="5,0"
                                          Data="M 0,0 L 5,5 L 10,0"
                                          Stroke="{TemplateBinding Foreground}"
                                          StrokeThickness="2">
                                        <Path.RenderTransform>
                                            <RotateTransform Angle="0" CenterX="5" CenterY="3"/>
                                        </Path.RenderTransform>
                                    </Path>
                                    <!-- 标题内容 -->
                                    <ContentPresenter Grid.Column="1"
                                                      Content="{TemplateBinding Header}"
                                                      ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                      Margin="5"/>
                                </Grid>
                            </ToggleButton>

                            <!-- 可动画的内容容器 -->
                            <Grid x:Name="ContentGrid" 
                                  Grid.Row="1">
                                <Grid.LayoutTransform>
                                    <ScaleTransform ScaleY="0"/>
                                </Grid.LayoutTransform>
                                <ContentPresenter x:Name="ContentSite"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!-- 缩放动画 -->
                                        <DoubleAnimation Storyboard.TargetName="ContentGrid"
                                                       Storyboard.TargetProperty="LayoutTransform.ScaleY"
                                                       From="0" To="1"
                                                       Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <!-- 箭头旋转动画 -->
                                        <DoubleAnimation Storyboard.TargetName="Arrow"
                                                       Storyboard.TargetProperty="RenderTransform.Angle"
                                                       From="0" To="180"
                                                       Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ContentGrid"
                                                       Storyboard.TargetProperty="LayoutTransform.ScaleY"
                                                       From="1" To="0"
                                                       Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseIn"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="Arrow"
                                                       Storyboard.TargetProperty="RenderTransform.Angle"
                                                       From="180" To="0"
                                                       Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 更简单的平滑高度动画样式 -->
    <Style x:Key="SmoothAnimatedExpander" TargetType="Expander">
        <Setter Property="BorderBrush" Value="LightGray"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 标题区域 -->
                        <Border Grid.Row="0"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ToggleButton x:Name="HeaderButton"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        HorizontalContentAlignment="Stretch"
                                        IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter Grid.Column="0"
                                                      Content="{TemplateBinding Header}"
                                                      Margin="10,8"/>

                                    <Path x:Name="ExpanderArrow"
                                          Grid.Column="1"
                                          Width="8"
                                          Height="8"
                                          Margin="10,0"
                                          Stretch="Uniform"
                                          Fill="{TemplateBinding Foreground}"
                                          Data="M 0,2 L 4,6 L 8,2 Z"/>
                                </Grid>
                            </ToggleButton>
                        </Border>

                        <!-- 内容区域（带有动画） -->
                        <ContentPresenter x:Name="ContentPresenter"
                                        Grid.Row="1"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        Visibility="Collapsed"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <!-- 展开时显示内容 -->
                            <Setter TargetName="ContentPresenter" 
                                    Property="Visibility" 
                                    Value="Visible"/>
                            <!-- 箭头旋转 -->
                            <Setter TargetName="ExpanderArrow" 
                                    Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180" CenterX="4" CenterY="4"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- 动画触发器 -->
            <EventTrigger RoutedEvent="Expander.Expanded">
                <BeginStoryboard>
                    <Storyboard>
                        <!-- 淡入动画 -->
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0" To="1"
                                         Duration="0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>

            <EventTrigger RoutedEvent="Expander.Collapsed">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="1" To="0"
                                         Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>